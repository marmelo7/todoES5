const input = document.querySelector("#input");
const container = input.parentNode.parentNode;

input.addEventListener("keydown", function(e){
    if (e.keyCode == 13){
        input.parentNode.parentNode.append
    }
});